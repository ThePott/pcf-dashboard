services:
    app:
        build: .
        ports:
            - "3000:3000"
        environment:
            DATABASE_URL: postgresql://postgres:password@db:5432/almond
        depends_on:
            - db
    db:
        image: postgres:18
        environment:
            POSTGRES_PASSWORD: password
            POSTGRES_DB: almond
